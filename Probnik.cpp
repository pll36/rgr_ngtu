#include <iostream>

using namespace std;

int main() {
    int variable; // переменная для проверки

    // Пример: присвоим значение или получим из ввода
    // cin >> variable;

    switch (variable) {
    case 1:
        // действие для значения 1
        break;
    case 2:
        // действие для значения 2
        break;
        // добавьте нужные case
    default:
        // действие по умолчанию
        break;
    }

    return 0;
}